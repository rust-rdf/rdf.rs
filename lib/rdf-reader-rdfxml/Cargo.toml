[package]
name = "rdf-reader-rdfxml"
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "An RDF/XML file reader for RDF.rs knowledge graphs."
#documentation.workspace = true
readme = "README.md"
homepage.workspace = true
repository.workspace = true
license.workspace = true
keywords.workspace = true
categories.workspace = true
publish.workspace = true

[package.metadata.lvr]
label = "RDF/XML"
summary = "An RDF/XML file reader for [RDF.rs] knowledge graphs."
features = []

[package.metadata.lvr.examples]
import = "rdf_reader_rdfxml::RdfxmlReader"

[features]
default = ["all", "std"]
all = ["oxrdf"]
alloc = ["rdf-format/alloc", "rdf-model/alloc", "rdf-reader/alloc"]
std = ["alloc", "rdf-format/std", "rdf-model/std", "rdf-reader/std", "futures/std", "dep:async-stream", "dep:tokio"]
unstable = []

# Implementations:
oxrdf = ["std", "dep:oxrdfxml", "rdf-format/oxrdf", "rdf-model/oxrdf", "rdf-reader/oxrdf"]

[dependencies]
async-stream = { workspace = true, optional = true }
futures.workspace = true
rdf-format.workspace = true
rdf-model.workspace = true
rdf-reader.workspace = true
thiserror.workspace = true
tokio = { workspace = true, optional = true, features = ["io-util"] }

# Implementations:
oxrdfxml = { workspace = true, optional = true }

[dev-dependencies]
tokio = { workspace = true, features = ["fs", "rt-multi-thread", "macros"] }

[[example]]
name = "read_rdfxml_iter"
required-features = ["oxrdf"]

[[example]]
name = "read_rdfxml_stream"
required-features = ["oxrdf"]

# Preview locally with: RUSTDOCFLAGS="--cfg docsrs" cargo +nightly doc --all-features
[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]
