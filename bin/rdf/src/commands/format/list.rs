// This is free and unencumbered software released into the public domain.

use crate::{exit::ExitCode, StandardOptions};

#[allow(dead_code)]
pub fn list(_flags: &StandardOptions) -> Result<(), ExitCode> {
    unimplemented!("`rdf format list` is not implemented yet") // TODO
}
